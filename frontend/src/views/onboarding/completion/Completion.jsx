import React, { useState } from "react";
import { Redirect } from "react-router-dom";

const Completion = ({}) => {
  return <div className="completion-container form-container">Completion</div>;
};

export default Completion;
