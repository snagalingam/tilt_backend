import React, { useState } from "react";
import { Redirect } from "react-router-dom";

const Parent = ({ next, flowIndex, setAnswers }) => {
  return <div className="parent-container form-container">Parent</div>;
};

export default Parent;
