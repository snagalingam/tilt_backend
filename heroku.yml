setup:
  addons:
    - plan: heroku-postgresql

build:
  docker:
    web: Dockerfile

release:
  image: web
  command:
    - python backend/manage.py collectstatic --noinput

run:
  web: PYTHONPATH="$PWD/backend/" gunicorn tilt_project.wsgi
