env:
  - AWS_BUCKET=financial-letters
  - AWS_LAMBDA_FUNCTION=wait-for-textract
  - AWS_REGION=us-east-2
  - ENVIRONMENT=development
  - GRAPHQL_ENDPOINT=https://localhost:8000/graphql
  - DATABASE_HOST=db
  - DATABASE_NAME=postgres
  - DATABASE_PASSWORD=postgres
  - DATABASE_USER=postgres
  - DEBUG=1
  - SECRET_KEY=rUDX9ep7!&@YszbBaREjM2zAXqydPmvCz%Z5Hh4xBAdu3!V
  - TWILIO_PHONE_NUMBER=+15624721665
  - SENDER_NAME=Tilt
sudo: required
services:
  - docker

before_install:
  - docker build -t snagalingam/tilt-app-test -f ./app/Dockerfile.dev ./app

script:
  - docker run snagalingam/tilt-app-test python manage.py test

after_success:
  - docker build -t snagalingam/tilt-app ./app
  - docker build -t snagalingam/tilt-nginx ./nginx
  # log in to the docker cli
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # take those images and push them to docker hub
  - docker push snagalingam/tilt-app
  - docker push snagalingam/tilt-nginx
